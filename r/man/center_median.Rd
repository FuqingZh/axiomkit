% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-quant_matrix.r
\name{center_median}
\alias{center_median}
\title{Median-Center a Quantification Matrix}
\usage{
center_median(
  x,
  rule_axis = c("col", "row"),
  rule_baseline = c("global_median", "zero")
)
}
\arguments{
\item{x}{A \code{QuantMatrix} object.}

\item{rule_axis}{Axis for centering: \code{"col"} or \code{"row"}.
\itemize{
\item \code{"col"}: center each column by its median.
\item \code{"row"}: center each row by its median.
}}

\item{rule_baseline}{Baseline to add after centering.
\itemize{
\item \code{"global_median"}: add back the global median of axis medians.
\item \code{"zero"}: add back \code{0} (pure centering).
}}
}
\value{
A new \code{QuantMatrix} object after median centering.
}
\description{
Center by axis medians, with optional baseline re-alignment.
}
\details{
For each element \code{X[i, j]}, the transform is:
\verb{X'[i, j] = X[i, j] - m_axis + b}
where \code{m_axis} is the row/column median selected by \code{rule_axis},
and \code{b} is determined by \code{rule_baseline}.
}
